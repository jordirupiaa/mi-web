/**
 * Non-text factual data about Hotel Casa Mas that isn't part of the
 * translated UI copy (src/i18n/locales/*.json holds that instead). This file
 * only holds values that aren't language-dependent strings — a number, and a
 * list of card brand names that don't get translated.
 */

export const TOTAL_ROOMS = 40

/** Card brand names are proper nouns — not translated per language. */
export const POLICIES = {
  cards: ['Visa', 'MasterCard', 'Carte Bleue'],
}
